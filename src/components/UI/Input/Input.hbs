<input 
    id="{{id}}"
    class="input {{#if class}}{{class}}{{/if}}" 
    name="{{id}}"
    type="{{#if type}}{{type}}{{else}}text{{/if}}" 
    placeholder="{{placeholder}} "
    value="{{value}}"
    >
